The `docker/images` directory contains the Docker image built by
`scripts/run_in_container.py`.

To reproduce the exact results from the thesis, place the pre-built Docker
images into this directory.
